@keyframes sparkle
	0%
		fill darkblue
	50%
		fill lightblue
	100%
		fill darkblue

@keyframes wiggle
	50%
		transform translateX(5px)
	100%
		transform translateX(-5px)
		
@keyframes fadeIn
	0%
		transform scale(2)
		opacity 0
	50%
		opacity 1
	100%
		transform scale(1)